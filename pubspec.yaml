name: flutter_nude_detector
description: Flutter package to detect nudity content in images
version: 0.0.1

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: 3.3.0

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_image_labeling: ^0.5.0
  path: any
  path_provider: ^2.0.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^3.1.0
